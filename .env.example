# Leave both values empty to use the clearly labelled local demo.
# Set both at BUILD time to enable the live directory.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
# Use only the public sb_publishable_ key. Never put a service-role/secret key here.
